Start 
Initialization(Setting up the environment for frontend and backend process)
Backend Setup
Add data to JavaScript 
Define a list of bus schedules
Create a route to serve the main page
Create an API endpoint to return the bus schedules in JSON format
Frontend Setup
Create an HTML template to display the bus schedule.
Include JavaScript to fetch the bus schedules from the API and dynamically display them on the page.
Stop
